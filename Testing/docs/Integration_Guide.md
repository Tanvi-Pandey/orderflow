# Frontend and Backend Integration

Backend URL:
http://localhost:8080

Frontend URL:
http://localhost:5173

React sends HTTP requests to the Spring Boot backend.

The backend processes the requests and returns JSON responses.